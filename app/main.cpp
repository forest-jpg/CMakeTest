#include <bits/stdc++.h>
#include "my_lib.h"
using namespace std;
using ll = long long;
#define rep(i, s, n) for (int i = s; i < (int)(n); i++)
int main() {
    print_hello_world();
    return 0;
}